Module status: bug-found+fix. Pre-fix `train/grpo/rewards.py` used a process-global Welford normalizer in `_RunningNorm` (original lines 34-52), allocated one norm per component once (original line 66), and updated it during every scalar `compute()` call (original lines 115-127), making rewards order-, restart-, and rank-dependent; component wiring was preserved in the fixed file (`_r_anc` lines 117-123, `_r_rep` lines 125-136, `_r_phr` lines 163-167, `_FNS` lines 169-172). The fix makes `per_group` the default normalization mode (lines 84-99), adds reset/checkpoint support for legacy running state (lines 36-67 and 101-114), adds order-invariant per-batch component standardization and weighted aggregation (lines 188-235), and documents that the legacy `running` path is only for single-process torch plus the double-normalization interaction with trainer group-standardized advantages (`train/grpo/trainer.py` lines 155-156). TDD evidence: red-on-original was saved in `.sisyphus/evidence/task-w1t1-pytest-red.txt` (missing `compute_group`/state APIs and demonstrating the old order-dependence contract); green-after-fix is saved in `.sisyphus/evidence/task-w1t1-pytest.txt`.
